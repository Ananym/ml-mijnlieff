# AWS Configuration
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# AWS Account ID (12-digit number)
# Used by CDK and deploy scripts
# If not set, will attempt to get from AWS CLI profile
AWS_ACCOUNT_ID=

# AWS Region (default: eu-west-2)
AWS_REGION=eu-west-2

# Lambda Function URL (output from CDK deploy)
# Used when building frontend locally
# For GitHub Actions, set this as a repository variable: LAMBDA_FUNCTION_URL
VITE_API_ENDPOINT=
